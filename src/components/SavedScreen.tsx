import { Bookmark, Star, Trash2 } from 'lucide-react';
import type { Course } from '../types';
export function SavedScreen({ courses, onRemove, onOpen, onExplore }: { courses: Course[]; onRemove:(id:string)=>void; onOpen:(c:Course)=>void; onExplore:()=>void }) {
 return <section className="screen-section"><div className="screen-heading"><div><p className="eyebrow">Your shortlist</p><h1>Saved courses</h1><p>Keep promising options close while you build your schedule.</p></div></div>{courses.length===0?<div className="empty-screen card"><Bookmark size={42}/><h2>Your list is waiting</h2><p>Save courses from Explore and they’ll stay here on this device.</p><button className="button primary" onClick={onExplore}>Find courses</button></div>:<div className="saved-list">{courses.map(c=><article className="saved-row card" key={c.id}><div><span className="code-chip">{c.code}</span><h3>{c.title}</h3><p>{c.professor} · {c.semester} {c.year}</p></div><div className="saved-metrics"><span><Star size={15} fill="currentColor"/> {c.avgRating}</span><span>{c.workloadHours}h/week</span><span>{c.difficulty}/5 difficulty</span></div><div className="row-actions"><button className="button ghost compact" onClick={()=>onOpen(c)}>Open</button><button className="icon-button" onClick={()=>onRemove(c.id)} aria-label="Remove saved course"><Trash2 size={17}/></button></div></article>)}</div>}</section>;
}
